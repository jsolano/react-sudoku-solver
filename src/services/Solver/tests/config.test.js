import { parseGrid } from '../../Solver/solver';

import { initialValuesState } from './data';

describe('config', () => {
	it('getBoardState', () => {
		// const values = parseGrid(initialSudokuString);
		// expect(getBoardState(values)).toEqual(
		// 	expect.objectContaining(initialValuesState)
		// );
	});

	// it('validSectionRows', () => {
	// 	const expected = [1, 2, 3];
	// 	expect(validSectionRows).toEqual(expect.arrayContaining(expected));
	// });

	// it('emptySudokuString', () => {
	// 	const expected = '.'.repeat(81);
	// 	expect(emptySudokuString).toEqual(expected);
	// });

	// it('initialSudokuString', () => {
	// 	expect(initialSudokuString.length).toEqual(81);
	// 	expect(initialSudokuString).toEqual(
	// 		expect.stringMatching(acceptedStringRegExp)
	// 	);
	// });

	// it('parseGrid', () => {
	// 	expect(parseGrid(initialSudokuString)).toEqual(
	// 		expect.arrayContaining(parsedGrid1)
	// 	);
	// });

	// it('getBoardState', () => {
	// 	const values = parseGrid(initialSudokuString);
	// 	const msg = 'Foo';

	// 	expect(getBoardState(values)).toEqual(
	// 		expect.objectContaining(initialBoardState)
	// 	);
	// });
});
